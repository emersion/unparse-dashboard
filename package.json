{
  "name": "unparse-dashboard",
  "version": "0.1.0",
  "description": "A UI to manage Unparse data.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "node_modules/.bin/unparse init --config-file=config.json",
    "start": "node index.js",
    "postinstall": "npm run init"
  },
  "author": "emersion",
  "license": "MIT",
  "dependencies": {
    "express": "^4.13.0",
    "unparse": "unparse/node-unparse"
  }
}
